#include "MetadataManager.hpp"

Song MetadataManager::readMetadata(const std::string& filePath) {
    // Placeholder for metadata reading (e.g., from MP3)
    return Song("Unknown Title", "Unknown Artist", "Unknown Album", filePath);
}